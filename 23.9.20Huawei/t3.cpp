#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>
using namespace std;

unordered_map<string, int> hashtable;

void analysis(string& a) {
    if (a == "let") {
        string name;
        int val;
        cin >> name;
        cin >> val;
        insertSymbol(name, )
    }
}

int main() {
    string key;
    cin >> key; 
    analysis(key);
}